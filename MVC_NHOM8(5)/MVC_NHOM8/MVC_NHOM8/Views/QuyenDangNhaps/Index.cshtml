@model IEnumerable<MVC_NHOM8.Models.QuyenDangNhap>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/SanPhams/Index.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenQuyenDN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenQuyenDN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.QuyenDNID }) |
            @Html.ActionLink("Details", "Details", new { id=item.QuyenDNID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.QuyenDNID })
        </td>
    </tr>
}

</table>
